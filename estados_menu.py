from enum import Enum, auto


class MenuState(Enum):
    MAIN = auto()
    OPTIONS = auto()
    VIDEO_SETTINGS = auto()
    AUDIO_SETTINGS = auto()
    KEYS_SETTINGS = auto()
    SIMULACAO = auto()
    SIMULATION_SETTINGS = auto()
